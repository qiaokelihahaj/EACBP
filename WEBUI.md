# EACBP WebUI

本地、单用户的分析工作台。界面和 HTTP 服务使用 Python 标准库与静态页面，不需要安装 Node.js、构建前端或额外安装 Web 框架；科学计算仍需要原有的可选依赖。

## 启动

当前 Windows 源码环境：

```powershell
.\.venv\Scripts\python.exe -m eacbp webui
```

安装包后：

```sh
eacbp webui --workspace /path/to/data --runs-dir /path/to/outputs/runs --port 8765
```

默认自动打开浏览器，地址为 `http://127.0.0.1:8765/`。`--no-browser` 只启动服务。默认输入工作目录为当前目录，输出及历史目录为 `工作目录/outputs/runs`。端口被占用时可使用 `--port 8766`。

界面仅绑定 `127.0.0.1`，不提供公网或多用户服务。HTTP API 校验每次启动生成的页面令牌、Host 与请求来源；页面无远程脚本、字体或前端遥测。服务器重启后需刷新页面获取新会话。此机制不是针对同机不受信任用户的账户权限隔离。

在服务器上使用时，在允许执行计算的节点启动 WebUI。它直接运行当前机器上的 Python 作业，**不会代为提交 Slurm，也不应在登录节点执行重计算**。本机可通过 SSH 隧道访问，例如：

```sh
ssh -N -L 8765:127.0.0.1:8765 user@compute-host
```

在本机浏览器访问相同端口 `http://127.0.0.1:8765/`。`compute-host` 应替换为实际允许连接的节点或已有 SSH 配置；需要跳板时遵循集群配置。本轮未验证真实服务器、SSH 隧道或 Slurm 作业。

## 使用流程

1. **选择数据**：浏览工作目录内的 `.h5ad`，或直接输入路径。路径和符号链接解析后必须仍在工作目录内。大文件直接由后台读取，不经浏览器上传。文件列表隐藏点目录与临时目录；显式输入路径仍可读取工作目录内的文件。
2. **检查结构**：显示细胞/基因数、layer、空间坐标标记和元数据列。这里只检查结构，不验证计数及生物学合理性。AnnData 使用 backed 模式读取；部分元数据与 layers 仍可能产生内存开销。
3. **填写研究设计**：物种、组织、目标细胞类型、条件对比、元数据列名。条件 A/B 同时填写或同时留空，不推测配对设计。
4. **选择方法**：默认标准模式。高级统计单独启用 PyDESeq2；配对选项要求选用该模型。没有合理的根细胞时留空，标准 DPT 分支将在数据审计后省略。
5. **预览再启动**：复用原计划器，展示具体方法、缺失参数和最终配置。静态预览不保证输入内容、所有外部可执行程序或计算资源可用。修改配置后必须重新预览。
6. **查看结果**：运行记录提供最近任务事件、进程日志片段、保存的报告、配置和摘要。可下载 Markdown 报告与 JSON 文件。
7. **恢复或校验**：“恢复运行”使用原研究目录与配置；“校验并重建报告”调用原快照/审计校验逻辑。文件被修改、审计被撤销或恢复指纹不符时，原有拒绝语义仍保留。

“导出设置”与“导入设置”读写 `{"webui_schema": 1, "form": {...}}` 格式，用于复用页面参数。输入数据不包含在设置文件中。高级配置框接收普通 `RunConfig` JSON，递归覆盖基础配置，不会静默把简化方法替换为高级方法。外部资源路径以工作目录为基准解析；参考模型/网络仍由使用者提供。WebUI 首版新建只支持 h5ad，FASTQ 继续使用现有脚本。

## 运行与恢复边界

- 每次新建生成独立运行目录。历史发现覆盖输出根目录下三层以内含 `run_config.json` 的目录，兼容统一 CLI 和标准研究脚本布局。
- 科学计算在独立 Python 进程执行，复用 `eacbp.cli.run_study`、`resume_study`、`report_study`。关闭页面不会停止作业；停止 WebUI 服务也不主动终止 worker。完整日志和作业请求位于 `runs/.webui/jobs/<job-id>/`。
- 同一输出根目录下，WebUI 每次只允许一个后台操作。跨进程锁防止多个工作台同时提交；已有 CLI 执行继续受原运行目录锁约束。没有作业排队、取消进程树、Slurm 调度或自动资源分配功能。
- 作业状态持久化。worker 存活由操作系统文件锁识别；进程异常退出会显示“已中断”。启动中的作业最多保留 120 秒准备状态，超时且无 worker 锁时标记中断。损坏的作业记录显示“状态待检查”并阻止新提交，避免重复计算。
- 恢复只适用于已完整导入的数据。修改源码或依赖后，历史运行的严格恢复可能按原规则失败；WebUI 不关闭完整性检查来绕过此限制。
- 默认报告标签为“已保存的副本”，不代表每次打开页面都重新校验了底层数据。“校验并重建报告”的成功仅表示该次报告校验完成；计算失败与科学质量告警仍按原报告呈现。
- 事件只展示最近片段；界面不声称其构成完整事件审计或可靠的整体进度百分比。日志/报告预览分别最多读取约 96 KB / 2 MB；完整结果保留在运行目录。浏览器下载仅开放报告、manifest、配置和摘要，最大 32 MB。
- 首版没有自动下载模型或修改研究数据的专用入口。高级配置会调用现有能力，使用者应像使用 CLI 一样只提供可信的本地资源和可执行程序。

## 验证

`tests/test_webui.py` 覆盖表单边界、原计划器复用、路径限制、Host/来源/令牌校验、打包静态资源、重复提交保护、启动失败和异常退出状态，以及真实 h5ad/独立子进程运行、恢复不重新导入、报告重建和产物篡改拒绝。

浏览器验收使用明确命名的合成数据，验证文件选择、元数据检查、标准模式执行、页面刷新后的历史恢复和报告操作；这属于软件验收，不证明真实生物学有效性。

2026-09-20 验收记录：

- 完整回归 **312 passed、0 failed、0 skipped**，219.78 秒，419 条已有科学库及合成数据相关警告；结果为 `outputs/webui_validation/tests.xml`。
- 浏览器标准流程使用 120 cells × 240 genes 合成数据完成 7 个任务；恢复复用全部 7 个任务；报告校验重建成功。已检查桌面与窄屏布局。
- 离线构建 wheel，在独立目标目录安装后，从源码工作目录之外验证 HTML、JavaScript、CSS 和需令牌的设置 API，均通过；使用现有科学依赖，不代表全新机器的依赖安装验收。
- 全量回归后的可选 JSON null 配置边界修正另做 WebUI/CLI 针对性回归，验证可选字段默认值及非法类型拒绝。
